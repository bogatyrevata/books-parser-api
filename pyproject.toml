[tool.ruff]
# Не проверяем автогенерированные Alembic-миграции —
# их синтаксис (Union, Sequence) генерируется инструментом, руками не правим
exclude = ["migrations"]

[tool.ruff.lint]
ignore = [
    "B008",  # Depends(...) в дефолтах аргументов — стандартный паттерн FastAPI, не баг
]